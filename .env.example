# ===========================================
# ClinQflow Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# -----------------------------
# Database (Required)
# -----------------------------
# For Supabase: Use the connection string from Project Settings > Database
# For other providers: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/clinqflow?schema=public

# Supabase (if using Supabase)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -----------------------------
# Authentication (Required)
# -----------------------------
JWT_SECRET=change-this-to-a-secure-random-string-at-least-32-chars

# -----------------------------
# API Server
# -----------------------------
API_PORT=4005
CORS_ORIGIN=http://localhost:3005
NODE_ENV=development

# -----------------------------
# Web Frontend
# -----------------------------
NEXT_PUBLIC_API_BASE_URL=http://localhost:4005

# ===========================================
# Provider Configuration
# ===========================================
# The app uses a provider abstraction layer that allows you to
# swap service implementations via environment variables.
# Just change the provider type and configure its credentials.

# -----------------------------
# AI Provider
# -----------------------------
# Options: openai (default), gemini, claude
AI_PROVIDER=openai

# OpenAI Configuration
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Google Gemini Configuration (if using gemini)
# GOOGLE_AI_API_KEY=
# GEMINI_MODEL=gemini-1.5-flash

# Anthropic Claude Configuration (if using claude)
# ANTHROPIC_API_KEY=
# CLAUDE_MODEL=claude-3-sonnet-20240229

# -----------------------------
# Storage Provider
# -----------------------------
# Options: local (default), s3
STORAGE_PROVIDER=local

# Local Storage Configuration
LOCAL_STORAGE_PATH=./uploads

# AWS S3 Configuration (if using s3)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=
# AWS_REGION=us-east-1
# S3_ENDPOINT= # For MinIO or other S3-compatible services

# -----------------------------
# Cache Provider
# -----------------------------
# Options: memory (default), redis
CACHE_PROVIDER=memory

# Redis Configuration (if using redis)
# REDIS_URL=redis://localhost:6379
# REDIS_KEY_PREFIX=myapp

# -----------------------------
# Email Provider
# -----------------------------
# Options: console (default), sendgrid
EMAIL_PROVIDER=console

# SendGrid Configuration (if using sendgrid)
# SENDGRID_API_KEY=
# EMAIL_FROM=noreply@yourapp.com

# -----------------------------
# GitHub Integration (if enabled)
# -----------------------------
# GITHUB_PAT=your-personal-access-token

# ===========================================
# Legacy Configuration (deprecated, use providers above)
# ===========================================
# These are kept for backwards compatibility but will be removed in future versions.
# Please migrate to the provider configuration above.
